import 'package:flutter/material.dart';

import 'index.dart';

class GSTDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return GSTDetailView();
  }
}
